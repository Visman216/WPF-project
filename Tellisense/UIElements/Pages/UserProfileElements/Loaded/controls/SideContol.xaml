<UserControl x:Class="Tellisense.SideContol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tellisense"
             mc:Ignorable="d">
    <Border Margin="20 20 20 0" Width="200" VerticalAlignment="Top" BorderThickness="1" BorderBrush="White" >
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" Opacity="1" Direction="0"/>
        </Border.Effect>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Style="{StaticResource AAA}" Command="{Binding Command1}"/>
            <Button Grid.Row="1" Style="{StaticResource AAA}" Command="{Binding Command2}"/>
            <Button Grid.Row="2" Style="{StaticResource AAA}" Command="{Binding Command3}"/>
            <Button Grid.Row="3" Style="{StaticResource AAA}" Command="{Binding Command4}"/>
        </Grid>
    </Border>


</UserControl>
